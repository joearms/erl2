defMods  mod1 end.

addMod mod1. 

A = aaaaaaaaaaaaaaaaaaaaaaaaaaaa.
B = bbbbbbbbbbbbbbbbbbbbbbbbbbbb.

def foo(X,1) -> X + A;
    foo(P,2) -> P + B;
    foo(A,X) -> A + X + B;
    foo(A,B) -> A + B
end.

def foo1(X) ->
      F = fun(A) ->
             A + X + B
	  end,
      F(X, A) end.

erl2:dump().
erl2:make_mods().
	






